import processing.core.PImage;

import java.awt.*;

public class Explosion extends Entity{
    public Explosion(Point position, PImage image, WorldModel world) {
        super(position, image, world);
    }
}

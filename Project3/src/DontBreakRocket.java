import processing.core.PImage;

import java.awt.*;

public class DontBreakRocket extends DontBreak{
    public DontBreakRocket(Point position, PImage image, WorldModel world) {
        super(position, image, world);
    }
}

import processing.core.PImage;

import java.awt.*;

public class A_Spawn extends DontBreakRocket{
    public A_Spawn(Point position, PImage image, WorldModel world) {
        super(position, image, world);
    }
}
